from django.urls import path

# API routes for SOS, panic status, and recorded file upload will go here.

urlpatterns = [
    # path('panic-status/', PanicStatusView.as_view(), name='panic-status'),
]
